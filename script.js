$(document).keypress(function(e) {
  console.log('keypress', e.keyCode);

  /*
  switch (e.keyCode) {
    case 112:
      // 'p' key
      playPauseMusic();
      break;
    case 115:
      // 's' key
      volumeDown(3);
      break;
    case 119:
      // 'w' key
      volumeUp(3);
      break;
    case 100:
      // 'd' key
      skipForward();
      break;
    case 97:
      skipBack();
      break;
  }
  */
});
